plugins {
    id 'java'
}

group = 'io.waterwave'
version = '1.0'
archivesBaseName = 'Waterwave'

sourceCompatibility = targetCompatibility = 1.8
compileJava.options.encoding = 'UTF-8'

configurations {
    embed
    compile.extendsFrom(embed)
}
